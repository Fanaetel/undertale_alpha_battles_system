time ++;

switch (index) {
  case 0 :	
	if (time == 1)
		arena.set_size(300,200);
	 
	if (time > 400)
		destroy();	
	break;
 	
};

switch (time) {
	case 30 :
		ScrSpaw(500, 200, o_gasterblaster, 3, 10)
		break;

}